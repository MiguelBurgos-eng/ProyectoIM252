@{
    ViewData["Title"] = "Ventas > Detalle";
}

@model Domain.Entities.Venta

<dl>
    <dt>Id</dt>
    <dd>@Model.Id</dd>

    <dt>Cliente</dt>
    <dd>@Model.Cliente.Nombre</dd>
    
    <dt>Cliente Id</dt>
    <dd>@Model.ClienteId</dd>

    <dt>Correo del Cliente</dt>
    <dd>@Model.Cliente.Correo</dd>

    <dt>Foto del Cliente</dt>
    <dd>@Model.Cliente.Direccion</dd>

    <dt>Producto</dt>
    <dd>@Model.Producto.Descripcion</dd>

    <dt>Producto Id</dt>
    <dd>@Model.ProductoId</dd>

    <dt>Foto del Producto</dt>
    <dd><img src="@Model.Producto.Foto" /></dd>

    <dt>Fecha de Asignación</dt>
    <dd>@Model.Fecha.ToShortDateString()</dd>
</dl>

<a asp-action="Index" class="btn btn-danger mt-3">Regresar</a>